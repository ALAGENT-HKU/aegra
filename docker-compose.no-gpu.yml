# No-GPU override – use this for local development without NVIDIA GPU
# Usage: docker compose -f docker-compose.custom.yml -f docker-compose.no-gpu.yml up
services:
  aegra:
    deploy:
      resources:
        reservations:
          devices: !reset []
