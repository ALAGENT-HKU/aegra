# No-GPU override - use this on hosts without NVIDIA GPU, including the current Aliyun deployment.
# Usage: docker compose -f docker-compose.custom.yml -f docker-compose.no-gpu.yml up
services:
  aegra:
    deploy:
      resources:
        reservations:
          devices: !override []
